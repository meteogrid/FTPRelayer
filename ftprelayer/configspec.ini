[main]
archive_dir = string(default=None)

[relayers]
    [[__many__]]
    host = string()
    paths = string_list(default=list())
    uploader = option('ftp', 'scp', default='ftp')
    processor = string(default=None)
    username = string(default='anonymous')
    password = string(default=None)
