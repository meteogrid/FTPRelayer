[relayers]
    [[sigym]]
    host = example.com
    paths = /var/foo/*, /var/zoo/*, /var/car/*
    uploader = ftp
    username = pepe
    password = pepe2

    [[sigym2]]
    host = example.com
    paths = /var/car/*, /var/zar/*
    uploader = scp
    username = pepe
    password = pepe2
